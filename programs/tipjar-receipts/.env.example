# Environment variables for the tipjar receipts Anchor program

# The Solana cluster to deploy to.  Use `devnet` for development and
# `mainnet` when deploying a production program.
SOLANA_CLUSTER=devnet

# Path to your wallet keypair file used for deploying the program.  This
# defaults to the Solana CLI configuration at ~/.config/solana/id.json.
ANCHOR_WALLET=~/.config/solana/id.json

# Program ID for the tipjar receipts program.  Leave blank until
# deployment.  After deploying with `anchor deploy --provider.cluster
# devnet`, copy the resulting program ID here and propagate it to the
# API and front‑end `.env` files.
TIPJAR_PROGRAM_ID=