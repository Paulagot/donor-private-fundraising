# Environment variables for the web front‑end

# RPC endpoint used by the front‑end when querying transactions.  Leave
# commented out to use the cluster specified by SHARED_RPC_URL in the root `.env`.
VITE_RPC_URL=https://api.devnet.solana.com

# Public key of the donation wallet.  Must match DONATION_SOL_ADDRESS in the
# API and root `.env` files.
VITE_DONATION_SOL_ADDRESS=YourDonationAddressHere

# Base URL of the API.  During local development the API runs on port 3001.
# In production, set this to your deployed API endpoint.
VITE_API_URL=http://localhost:3001